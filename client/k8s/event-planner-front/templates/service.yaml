apiVersion: v1
kind: Service
metadata:
  name: {{ include "event-planner-front.fullname" . }}
  labels:
    {{- include "event-planner-front.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "event-planner-front.selectorLabels" . | nindent 4 }}
