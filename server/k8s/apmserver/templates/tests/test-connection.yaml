apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apmserver.fullname" . }}-test-connection"
  labels:
    {{- include "apmserver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apmserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
